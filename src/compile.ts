// const sass = require('sass');
// const fs = require('fs');

// const result = sass.renderSync({
//   file: './styles/index.scss', // Remplacez par le chemin de votre fichier
//   outFile: 'style.css', // Spécifiez le nom du fichier CSS de sortie
//   sourceMap: true, // Génère une source map pour le débogage
//   silenceDeprecations: ['legacy-js-api']
// });

// fs.writeFileSync('style.css', result.css);
